# ==========================================
# CONFIGURACIÓN DE LA APLICACIÓN
# ==========================================
# Nombre de la aplicación (aparece en títulos y notificaciones)
APP_NAME=SIGAP
# Entorno de ejecución: local, production, staging
APP_ENV=local
# Clave de encriptación (se genera automáticamente con 'php artisan key:generate')
APP_KEY=
# Modo de depuración (true solo en desarrollo, false en producción)
APP_DEBUG=true
# URL base de la aplicación
APP_URL=http://localhost

# ==========================================
# CONFIGURACIÓN DE IDIOMA Y ZONA HORARIA
# ==========================================
# Idioma principal de la aplicación
APP_LOCALE=es
# Idioma de respaldo si no está disponible el principal
APP_FALLBACK_LOCALE=es
# Idioma para datos de prueba generados con Faker
APP_FAKER_LOCALE=es_ES
# Zona horaria de la aplicación
APP_TIMEZONE=America/El_Salvador

# ==========================================
# CONFIGURACIÓN DE MANTENIMIENTO
# ==========================================
APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

# ==========================================
# SEGURIDAD
# ==========================================
# Número de rondas de encriptación para contraseñas (mayor = más seguro pero más lento)
BCRYPT_ROUNDS=12

# ==========================================
# CONFIGURACIÓN DE LOGS
# ==========================================
# Canal de registro de eventos: stack, single, daily, syslog, errorlog
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
# Nivel de registro: debug, info, notice, warning, error, critical, alert, emergency
LOG_LEVEL=debug

# ==========================================
# CONFIGURACIÓN DE BASE DE DATOS
# ==========================================
# Tipo de base de datos: mysql, pgsql, sqlite, sqlsrv
DB_CONNECTION=sqlite
# Para MySQL/PostgreSQL, descomentar y configurar las siguientes líneas:
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=sigap
# DB_USERNAME=root
# DB_PASSWORD=

# ==========================================
# CONFIGURACIÓN DE SESIONES
# ==========================================
# Controlador de sesiones: file, cookie, database, memcached, redis
SESSION_DRIVER=database
# Tiempo de vida de la sesión en minutos
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# ==========================================
# DRIVERS DE APLICACIÓN
# ==========================================
# Broadcasting: log, pusher, ably, redis, null
BROADCAST_CONNECTION=log
# Sistema de archivos: local, public, s3
FILESYSTEM_DISK=local
# Cola de trabajos: sync, database, redis, sqs, null
QUEUE_CONNECTION=database

# ==========================================
# CONFIGURACIÓN DE CACHÉ
# ==========================================
# Almacén de caché: file, database, memcached, redis, dynamodb, octane, array
CACHE_STORE=database
# CACHE_PREFIX=

# ==========================================
# SERVICIOS EXTERNOS
# ==========================================
# Memcached
MEMCACHED_HOST=127.0.0.1

# Redis
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# ==========================================
# CONFIGURACIÓN DE CORREO ELECTRÓNICO
# ==========================================
# Mailer: smtp, sendmail, mailgun, ses, postmark, log, array, failover, roundrobin
MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
# Dirección de correo que aparece como remitente
MAIL_FROM_ADDRESS="noreply@sigap.local"
MAIL_FROM_NAME="${APP_NAME}"

# ==========================================
# AWS (Amazon Web Services)
# ==========================================
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# ==========================================
# VITE (Frontend Build Tool)
# ==========================================
VITE_APP_NAME="${APP_NAME}"

